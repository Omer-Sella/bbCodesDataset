#Dataset of Bicycle Bivariate quantum error correcting codes

For each \(l,m\) there is a collection of .json files containing code parameters, which error points they were sampled on, how many samples, what's the logical error rate observed using BP+OSD0

